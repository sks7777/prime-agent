- Added the `resumePendingUserMessage` prompt option (wire field, schema revision 34): a prompt-recovery re-issue whose transcript already ends with the same user message resumes that dangling turn instead of appending a duplicate — the dispatch continues the agent over the existing tail, settles the delivery ticket without re-emitting the message, and the model sees the prompt once. Unrelated same-text prompts still append normally.
