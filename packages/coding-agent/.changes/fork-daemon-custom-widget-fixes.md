- Fixed daemon-mode `ctx.ui.custom()` forwarding: multi-key widgets no longer break after the first key press, keybindings respect `keybindings.json`, widgets render at the client's real terminal width, and key listeners are cleaned up on detach. Key forwarding is gated on the new `extension_ui_key_events` daemon capability, so older daemons cancel the widget instead of leaving it dead.
