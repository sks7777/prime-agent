- Added bb mirror threads for RLM subagents: `rlm.spawn(bb_mirror=True)` defers the child's admission prompt, the ACP frontend rebinds onto the claimed subagent session via a single-use `[rlm-mirror:<nonce>]` claim (validated as an RLM subagent of the same working directory, TTL 10 min), and the new `bb_subagents.spawn_mirror` skill spawns the visible bb thread in one call.
- Fixed bb-mirror safety: a never-prompted deferred child settles on cancellation or a 10-minute timeout instead of wedging the parent's quiescence barrier, and failed mirror spawns delete the waiting child.
